#include "REL/m638Dll.h"

/* static */
MGSEQ_PARAM lbl_1_data_0 = {
    300,
    1,
    fn_1_1EC,
    fn_1_284,
    fn_1_528,
    fn_1_598,
    fn_1_77C,
    fn_1_A28,
    fn_1_D84,
    fn_1_11EC,
    fn_1_11F0,
};

char lbl_1_data_28[9] = { 102, 97, 95, 114, 100, 98, 48, 48, 0 };

char lbl_1_data_31[9] = { 102, 97, 95, 114, 100, 98, 48, 49, 0 };

char lbl_1_data_3A[9] = { 102, 97, 95, 114, 100, 98, 49, 48, 0 };

char lbl_1_data_43[9] = { 102, 97, 95, 114, 100, 98, 49, 49, 0 };

char lbl_1_data_4C[9] = { 102, 97, 95, 114, 100, 98, 50, 48, 0 };

char lbl_1_data_55[9] = { 102, 97, 95, 114, 100, 98, 50, 49, 0 };

char lbl_1_data_5E[9] = { 102, 97, 95, 114, 100, 98, 51, 48, 0 };

char lbl_1_data_67[9] = { 102, 97, 95, 114, 100, 98, 51, 49, 0 };

char lbl_1_data_70[9] = { 102, 98, 95, 114, 100, 98, 48, 48, 0 };

char lbl_1_data_79[9] = { 102, 98, 95, 114, 100, 98, 48, 49, 0 };

char lbl_1_data_82[9] = { 102, 98, 95, 114, 100, 98, 49, 48, 0 };

char lbl_1_data_8B[9] = { 102, 98, 95, 114, 100, 98, 49, 49, 0 };

char lbl_1_data_94[9] = { 102, 98, 95, 114, 100, 98, 50, 48, 0 };

char lbl_1_data_9D[9] = { 102, 98, 95, 114, 100, 98, 50, 49, 0 };

char lbl_1_data_A6[9] = { 102, 98, 95, 114, 100, 98, 51, 48, 0 };

char lbl_1_data_AF[9] = { 102, 98, 95, 114, 100, 98, 51, 49, 0 };

char lbl_1_data_B8[9] = { 102, 99, 95, 114, 100, 98, 48, 48, 0 };

char lbl_1_data_C1[9] = { 102, 99, 95, 114, 100, 98, 48, 49, 0 };

char lbl_1_data_CA[9] = { 102, 99, 95, 114, 100, 98, 49, 48, 0 };

char lbl_1_data_D3[9] = { 102, 99, 95, 114, 100, 98, 49, 49, 0 };

char lbl_1_data_DC[9] = { 102, 99, 95, 114, 100, 98, 50, 48, 0 };

char lbl_1_data_E5[9] = { 102, 99, 95, 114, 100, 98, 50, 49, 0 };

char lbl_1_data_EE[9] = { 102, 99, 95, 114, 100, 98, 51, 48, 0 };

char lbl_1_data_F7[9] = { 102, 99, 95, 114, 100, 98, 51, 49, 0 };

char lbl_1_data_100[9] = { 102, 100, 95, 114, 100, 98, 48, 48, 0 };

char lbl_1_data_109[9] = { 102, 100, 95, 114, 100, 98, 48, 49, 0 };

char lbl_1_data_112[9] = { 102, 100, 95, 114, 100, 98, 49, 48, 0 };

char lbl_1_data_11B[9] = { 102, 100, 95, 114, 100, 98, 49, 49, 0 };

char lbl_1_data_124[9] = { 102, 100, 95, 114, 100, 98, 50, 48, 0 };

char lbl_1_data_12D[9] = { 102, 100, 95, 114, 100, 98, 50, 49, 0 };

char lbl_1_data_136[9] = { 102, 100, 95, 114, 100, 98, 51, 48, 0 };

char lbl_1_data_13F[9] = { 102, 100, 95, 114, 100, 98, 51, 49, 0 };

char lbl_1_data_148[9] = { 102, 101, 95, 114, 100, 98, 48, 48, 0 };

char lbl_1_data_151[9] = { 102, 101, 95, 114, 100, 98, 48, 49, 0 };

char lbl_1_data_15A[9] = { 102, 101, 95, 114, 100, 98, 49, 48, 0 };

char lbl_1_data_163[9] = { 102, 101, 95, 114, 100, 98, 49, 49, 0 };

char lbl_1_data_16C[9] = { 102, 101, 95, 114, 100, 98, 50, 48, 0 };

char lbl_1_data_175[9] = { 102, 101, 95, 114, 100, 98, 50, 49, 0 };

char lbl_1_data_17E[9] = { 102, 101, 95, 114, 100, 98, 51, 48, 0 };

char lbl_1_data_187[9] = { 102, 101, 95, 114, 100, 98, 51, 49, 0 };

char *lbl_1_data_190[40] = {
    lbl_1_data_28,
    lbl_1_data_31,
    lbl_1_data_3A,
    lbl_1_data_43,
    lbl_1_data_4C,
    lbl_1_data_55,
    lbl_1_data_5E,
    lbl_1_data_67,
    lbl_1_data_70,
    lbl_1_data_79,
    lbl_1_data_82,
    lbl_1_data_8B,
    lbl_1_data_94,
    lbl_1_data_9D,
    lbl_1_data_A6,
    lbl_1_data_AF,
    lbl_1_data_B8,
    lbl_1_data_C1,
    lbl_1_data_CA,
    lbl_1_data_D3,
    lbl_1_data_DC,
    lbl_1_data_E5,
    lbl_1_data_EE,
    lbl_1_data_F7,
    lbl_1_data_100,
    lbl_1_data_109,
    lbl_1_data_112,
    lbl_1_data_11B,
    lbl_1_data_124,
    lbl_1_data_12D,
    lbl_1_data_136,
    lbl_1_data_13F,
    lbl_1_data_148,
    lbl_1_data_151,
    lbl_1_data_15A,
    lbl_1_data_163,
    lbl_1_data_16C,
    lbl_1_data_175,
    lbl_1_data_17E,
    lbl_1_data_187,
};

char lbl_1_data_230[5] = { 130, 200, 130, 181, 0 };

char lbl_1_data_235[9] = { 102, 97, 95, 115, 100, 98, 48, 48, 0 };

char lbl_1_data_23E[9] = { 102, 98, 95, 115, 100, 98, 48, 48, 0 };

char lbl_1_data_247[9] = { 102, 98, 95, 115, 100, 98, 48, 49, 0 };

char lbl_1_data_250[9] = { 102, 99, 95, 115, 100, 98, 48, 48, 0 };

char lbl_1_data_259[9] = { 102, 99, 95, 115, 100, 98, 48, 49, 0 };

char lbl_1_data_262[9] = { 102, 99, 95, 115, 100, 98, 48, 50, 0 };

char lbl_1_data_26B[9] = { 102, 100, 95, 115, 100, 98, 48, 48, 0 };

char lbl_1_data_274[9] = { 102, 100, 95, 115, 100, 98, 48, 49, 0 };

char lbl_1_data_27D[9] = { 102, 101, 95, 115, 100, 98, 48, 49, 0 };

char lbl_1_data_286[10] = { 102, 101, 95, 115, 100, 98, 48, 48, 0, 0 };

char *lbl_1_data_290[40] = {
    lbl_1_data_230,
    lbl_1_data_230,
    lbl_1_data_230,
    lbl_1_data_230,
    lbl_1_data_230,
    lbl_1_data_230,
    lbl_1_data_230,
    lbl_1_data_235,
    lbl_1_data_230,
    lbl_1_data_230,
    lbl_1_data_23E,
    lbl_1_data_230,
    lbl_1_data_230,
    lbl_1_data_247,
    lbl_1_data_230,
    lbl_1_data_230,
    lbl_1_data_230,
    lbl_1_data_230,
    lbl_1_data_250,
    lbl_1_data_230,
    lbl_1_data_259,
    lbl_1_data_230,
    lbl_1_data_262,
    lbl_1_data_230,
    lbl_1_data_26B,
    lbl_1_data_230,
    lbl_1_data_230,
    lbl_1_data_230,
    lbl_1_data_230,
    lbl_1_data_230,
    lbl_1_data_274,
    lbl_1_data_230,
    lbl_1_data_27D,
    lbl_1_data_230,
    lbl_1_data_230,
    lbl_1_data_230,
    lbl_1_data_286,
    lbl_1_data_230,
    lbl_1_data_230,
    lbl_1_data_230,
};

struct _struct_lbl_1_data_330_0x8 lbl_1_data_330[10] = {
    { 0.0f, 0.0f },
    { 245.0f, 10.0f },
    { 285.0f, 0.0f },
    { 287.0f, -10.0f },
    { 350.0f, 0.0f },
    { 362.0f, -10.0f },
    { 480.0f, 0.0f },
    { 505.0f, -10.0f },
    { 535.0f, 0.0f },
    { 600.0f, 0.0f },
};

f32 lbl_1_data_380[10] = { 0.0f, 0.0f, 0.0f, 14.67f, 11.5f, 0.0f, 6.2f, 22.45f, 0.0f, 0.0f };

s32 lbl_1_data_3A8[73] = {
    6356992,
    6356993,
    6356994,
    6356995,
    6356996,
    6356997,
    6356998,
    6356999,
    6357000,
    6357001,
    6357002,
    6357003,
    6357004,
    6357005,
    6357006,
    6357007,
    6357008,
    6357009,
    6357010,
    6357011,
    6357012,
    6357013,
    6357014,
    6357015,
    6357016,
    6357017,
    6357018,
    6357019,
    6357020,
    6357021,
    6357022,
    6357023,
    6357024,
    6357025,
    6357026,
    6357027,
    6357028,
    6357029,
    6357030,
    6357031,
    6357032,
    6357033,
    6357034,
    6357035,
    6357036,
    6357037,
    6357038,
    6357039,
    6357040,
    6357045,
    6357046,
    6357047,
    6357048,
    6357049,
    6357050,
    6357051,
    6357052,
    6357053,
    6357054,
    6357055,
    6357056,
    6357057,
    6357058,
    6357059,
    6357060,
    6357061,
    6357062,
    6357063,
    6357064,
    6357065,
    6357066,
    6357067,
    6357068,
};

char lbl_1_data_4CC[] = "l";

char lbl_1_data_4CE[] = "r";

char lbl_1_data_4D0[7] = { 114, 108, 101, 118, 101, 114, 0 };

char lbl_1_data_4D7[7] = { 108, 108, 101, 118, 101, 114, 0 };

char lbl_1_data_4DE[6] = { 100, 98, 95, 97, 98, 0 };

char lbl_1_data_4E4[7] = { 116, 101, 110, 98, 97, 110, 0 };

char lbl_1_data_4EB[4] = { 100, 98, 48, 0 };

char lbl_1_data_4EF[4] = { 100, 98, 49, 0 };

char lbl_1_data_4F3[4] = { 100, 98, 50, 0 };

char lbl_1_data_4F7[4] = { 100, 98, 51, 0 };

char lbl_1_data_4FB[7] = { 100, 98, 95, 115, 108, 48, 0 };

char lbl_1_data_502[7] = { 100, 98, 95, 115, 108, 49, 0 };

char lbl_1_data_509[7] = { 100, 98, 95, 115, 108, 50, 0 };

char lbl_1_data_510[7] = { 100, 98, 95, 115, 108, 51, 0 };

char lbl_1_data_517[9] = { 100, 98, 95, 115, 108, 52, 0, 0, 0 };

M638TimerWatch lbl_1_bss_10;

HUPROCESS *lbl_1_bss_C;

s32 lbl_1_bss_8;

OMOBJ *lbl_1_bss_4;

/* Four unreferenced retail BSS bytes; original declaration is unknown. */
u8 lbl_1_bss_0[4];

const u16 lbl_1_rodata_10[4] = { 1, 2, 4, 8 };

void fn_1_A0(void)
{
    OMOBJ *obj;
    s32 player;
    M638SceneView *scene;

    lbl_1_bss_C = omInitObjMan(10, 4096);
    omGameSysInit(lbl_1_bss_C);
    obj = omAddObjEx(lbl_1_bss_C, 101, 0, 0, -1, fn_1_1988);
    lbl_1_bss_4 = obj;
    obj->data = HuMemDirectMallocNum(HEAP_HEAP, sizeof(M638SceneView), 268435456U);
    scene = obj->data;
    for (player = 0; player < 4; player++) {
        obj = omAddObjEx(lbl_1_bss_C, 100, 1, 5, 0, fn_1_67EC);
        obj->data = HuMemDirectMallocNum(HEAP_HEAP, sizeof(M638PlayerView), 268435456U);
        obj->work[0] = player;
    }
    Hu3DBGColorSet(0, 0, 0);
    Hu3DNoSyncSet(1);
    fn_1_1CD4(scene);
    fn_1_1294();
    fn_1_165C(&scene->camera);
    fn_1_13B4((M638LightingView *)scene);
    fn_1_1470((M638LightingView *)scene);
    MgSeqCreate(&lbl_1_data_0);
}

void fn_1_1EC(s16 mode, s16 frameNo)
{
    M638SceneView *scene;

    scene = lbl_1_bss_4->data;
    scene->record = GWRecordGet(GW_RECORD_M638);
    if ((s32) scene->record == 0) {
        scene->record = 18000U;
    }
    scene->timer = MgTimerCreate(1);
    MgTimerParamSet(scene->timer, 0, 18000, (s32) scene->record);
    MgTimerRecordDispOn(scene->timer);
    fn_1_3804(scene);
    fn_1_1E58(scene);
    fn_1_4E48(scene);
    MgSeqModeNext();
}

void fn_1_284(s16 mode, s16 frameNo)
{
    f32 var_f31;
    M638SceneView *scene;

    scene = lbl_1_bss_4->data;
    switch (scene->introState) {
    case 0:
        MgSeqModeChangeOff();
        scene->teams[0].players[0]->impulse = 10.0f;
        scene->teams[0].players[1]->impulse = 10.0f;
        scene->teams[1].players[0]->impulse = 10.0f;
        scene->teams[1].players[1]->impulse = 10.0f;
        scene->teams[0].blend = 0.3f;
        scene->teams[1].blend = 0.3f;
        scene->seqTime = 0.0f;
        scene->camera.splitX = 640.0f;
        scene->introState++;
        break;
    case 1:
        scene->seqTime += 1.0f;
        if (scene->seqTime >= 90.0f) {
            scene->seqTime = 0.0f;
            scene->introState++;
        }
        break;
    case 2:
        scene->seqTime += 1.0f;
        var_f31 = scene->seqTime / 60.0f;
        if (var_f31 > 1.0f) {
            var_f31 = 1.0f;
        }
        scene->camera.splitX = (f32) ((640.0f * (1.0f - var_f31)) + (320.0f * var_f31));
        if (scene->seqTime >= 60.0f) {
            scene->seqTime = 0.0f;
            scene->introState++;
        }
        break;
    case 3:
        espAttrReset(scene->sprites[0], 4U);
        espAttrReset(scene->sprites[1], 4U);
        espAttrReset(scene->sprites[2], 4U);
        scene->camera.splitX = 320.0f;
        scene->introState++;
        /* fallthrough */
    case 4:
        scene->seqTime += 1.0f;
        if (scene->seqTime >= 100.0f) {
            MgSeqModeNext();
        }
        break;
    }
}

void fn_1_528(s16 mode, s16 frameNo)
{
    M638SceneView *scene;

    scene = lbl_1_bss_4->data;
    switch (scene->startState) {
    case 0:
        lbl_1_bss_8 = HuAudBGMPlay(79);
        scene->startState++;
        /* fallthrough */
    case 1:
        break;
    }
}

void fn_1_598(s16 mode, s16 frameNo)
{
    f32 motionTimes[2];
    s32 temp_r29;
    s32 temp_r30;
    M638SceneView *scene;

    scene = lbl_1_bss_4->data;
    switch (scene->playState) {
    case 0:
        MgTimerModeOnSet(scene->timer, 0);
        HuAudFXPlay(13);
        espAttrReset(scene->teams[0].sprites[0], 4U);
        espAttrReset(scene->teams[1].sprites[0], 4U);
        espAttrReset(scene->teams[0].sprites[1], 4U);
        espAttrReset(scene->teams[1].sprites[1], 4U);
        scene->teams[0].active = 1;
        scene->teams[1].active = 1;
        scene->playState++;
        break;
    case 1:
        temp_r30 = scene->teams[0].count == 10;
        temp_r29 = scene->teams[1].count == 10;
        if ((temp_r30 != 0) || (temp_r29 != 0)) {
            motionTimes[0] = Hu3DMotionTimeGet(scene->teams[0].course.model);
            motionTimes[1] = Hu3DMotionTimeGet(scene->teams[1].course.model);
            if ((temp_r30 != 0) && (temp_r29 != 0)) {
                if (motionTimes[0] > motionTimes[1]) {
                    scene->winner = 0;
                } else if (motionTimes[1] > motionTimes[0]) {
                    scene->winner = 1;
                } else {
                    scene->winner = frandmod(2);
                }
            } else if (temp_r30 != 0) {
                scene->winner = 0;
            } else {
                scene->winner = 1;
            }
            if (scene->winner == 0) {
                HuAudFXPlay(1893);
            } else if (scene->winner == 1) {
                HuAudFXPlay(1894);
            }
            MgTimerModeOffSet(scene->timer);
            scene->teams[0].active = 0;
            scene->teams[1].active = 0;
            MgSeqModeNext();
        }
        break;
    }
}

void fn_1_77C(s16 mode, s16 frameNo)
{
    s32 var_r30;
    M638SceneView *scene;

    scene = lbl_1_bss_4->data;
    switch (scene->finishState) {
    case 0:
        HuAudSStreamFadeOut(lbl_1_bss_8, 100);
        var_r30 = 0;
        while (var_r30 < 5) {
            espAttrSet(scene->teams[0].sprites[var_r30], 4U);
            espAttrSet(scene->teams[1].sprites[var_r30], 4U);
            var_r30 += 1;
        }
        scene->teams[0].active = 0;
        scene->teams[1].active = 0;
        scene->teams[0].elapsed = 0.0f;
        scene->teams[1].elapsed = 0.0f;
        scene->teams[0].base = (f32) scene->teams[0].blend;
        scene->teams[1].base = (f32) scene->teams[1].blend;
        scene->teams[0].state = 0;
        scene->teams[1].state = 0;
        if (scene->winner < 0) {
            MgSeqWinnerSet(-1, -1, -1, -1);
            fn_1_7660(fn_1_66C0, scene);
            fn_1_7660(fn_1_66C0, &scene->teams[1]);
        } else {
            espAttrSet(scene->sprites[0], 4U);
            espAttrSet(scene->sprites[1], 4U);
            espAttrSet(scene->sprites[2], 4U);
            MgSeqWinnerSet(scene->teams[scene->winner].players[0]->charNo, scene->teams[scene->winner].players[1]->charNo, -1, -1);
            scene->time = MgTimerValueGet(scene->timer);
            if ((scene->time < scene->record) && (_CheckFlag(65551U) == 0) && ((scene->teams[scene->winner].players[0]->isCom == 0) || (scene->teams[scene->winner].players[1]->isCom == 0))) {
                scene->newRecord = 1;
                MgSeqRecordSet(scene->time);
                GWRecordSet(GW_RECORD_M638, (u32) scene->time);
            }
            if (fn_1_7660(fn_1_6374, scene) != 0) {
                scene->camera.elapsed = 0.0f;
            }
            fn_1_7660(fn_1_647C, &scene->teams[scene->winner]);
            fn_1_7660(fn_1_66C0, &scene->teams[scene->winner ^ 1]);
            scene->teams[scene->winner].actor.blend = scene->teams[scene->winner].actor.rot.x;
            scene->teams[scene->winner].actor.mode = 0;
        }
        scene->finishState++;
        /* fallthrough */
    case 1:
        break;
    }
}

void fn_1_A28(s16 mode, s16 frameNo)
{
    f32 durations[2] = { 240.0f, 270.0f };
    M638SceneView *scene;

    scene = lbl_1_bss_4->data;
    scene->exitTime += 1.0f;
    switch (scene->resultState) {
    case 0:
        MgSeqModeChangeOff();
        if (scene->winner >= 0) {
            if (scene->newRecord != 0) {
                fn_1_7660(fn_1_6790, &lbl_1_bss_10);
                lbl_1_bss_10.count = 0;
                lbl_1_bss_10.timer = scene->timer;
            }
            scene->seqTime = 0.0f;
            scene->resultState = 1;
        } else {
            scene->resultState = 10;
        }
        break;
    case 1:
        scene->seqTime += 1.0f;
        if (scene->seqTime >= 30.0f) {
            scene->resultState++;
        }
        break;
    case 2:
        if (scene->teams[scene->winner].state == 2) {
            fn_1_7660(fn_1_6FE4, scene->teams[scene->winner].players[0]);
            fn_1_7660(fn_1_6FE4, scene->teams[scene->winner].players[1]);
            scene->resultState++;
        }
        break;
    case 3:
        if ((scene->teams[scene->winner].players[0]->exitState == 2) &&
            (scene->teams[scene->winner].players[1]->exitState == 2)) {
            scene->resultState = 100;
        }
        break;
    case 10:
        if (scene->teams[0].state == 2) {
            fn_1_7660(fn_1_6FE4, scene->teams[0].players[0]);
            fn_1_7660(fn_1_6FE4, scene->teams[0].players[1]);
            fn_1_7660(fn_1_6FE4, scene->teams[1].players[0]);
            fn_1_7660(fn_1_6FE4, scene->teams[1].players[1]);
            scene->resultState++;
        }
        break;
    case 11:
        if ((scene->teams[0].players[0]->exitState == 2) &&
            (scene->teams[0].players[1]->exitState == 2) &&
            (scene->teams[1].players[0]->exitState == 2) &&
            (scene->teams[1].players[1]->exitState == 2)) {
            scene->resultState = 100;
        }
        break;
    case 100:
        scene->seqTime = 0.0f;
        scene->resultState++;
        /* fallthrough */
    case 101:
        scene->seqTime += 1.0f;
        if (scene->seqTime >= 30.0f) {
            if (scene->winner < 0) {
                MgSeqModeNext();
            } else if (scene->exitTime > durations[scene->newRecord]) {
                MgSeqModeNext();
            } else {
                MgSeqModeChangeOn();
                scene->resultState++;
            }
        }
        break;
    case 102:
        break;
    }
}

void fn_1_D84(s16 mode, s16 frameNo)
{
    s32 temp_r30;
    s32 temp_r29;
    M638SceneView *scene;

    scene = lbl_1_bss_4->data;
    switch (scene->field6F8) {
    case 0:
        if (scene->winner < 0) {
            CharMotionShiftSet(scene->teams[0].players[0]->charNo,
                               scene->teams[0].players[0]->motions[3],
                               0.0f, 5.0f, 0U);
            CharMotionShiftSet(scene->teams[0].players[1]->charNo,
                               scene->teams[0].players[1]->motions[3],
                               0.0f, 5.0f, 0U);
            CharMotionShiftSet(scene->teams[1].players[0]->charNo,
                               scene->teams[1].players[0]->motions[3],
                               0.0f, 5.0f, 0U);
            CharMotionShiftSet(scene->teams[1].players[1]->charNo,
                               scene->teams[1].players[1]->motions[3],
                               0.0f, 5.0f, 0U);
            CharLoseVoicePlay(scene->teams[0].players[0]->charNo,
                              scene->teams[0].players[1]->charNo,
                              scene->teams[1].players[0]->charNo,
                              scene->teams[1].players[1]->charNo);
            Hu3DModelObjPosGet(scene->teams[0].actor.modelA,
                               lbl_1_data_4CC,
                               &scene->teams[0].players[0]->pos);
            Hu3DModelObjPosGet(scene->teams[0].actor.modelA,
                               lbl_1_data_4CE,
                               &scene->teams[0].players[1]->pos);
            scene->teams[0].players[0]->rot.y = 0.0f;
            scene->teams[0].players[1]->rot.y = 0.0f;
            Hu3DModelHookObjReset(scene->teams[0].actor.modelA,
                                  lbl_1_data_4CC);
            Hu3DModelHookObjReset(scene->teams[0].actor.modelA,
                                  lbl_1_data_4CE);
            fn_1_7554(scene->teams[0].players[0]);
            fn_1_7554(scene->teams[0].players[1]);
            Hu3DModelObjPosGet(scene->teams[1].actor.modelA,
                               lbl_1_data_4CC,
                               &scene->teams[1].players[0]->pos);
            Hu3DModelObjPosGet(scene->teams[1].actor.modelA,
                               lbl_1_data_4CE,
                               &scene->teams[1].players[1]->pos);
            scene->teams[1].players[0]->rot.y = 0.0f;
            scene->teams[1].players[1]->rot.y = 0.0f;
            Hu3DModelHookObjReset(scene->teams[1].actor.modelA,
                                  lbl_1_data_4CC);
            Hu3DModelHookObjReset(scene->teams[1].actor.modelA,
                                  lbl_1_data_4CE);
            fn_1_7554(scene->teams[1].players[0]);
            fn_1_7554(scene->teams[1].players[1]);
        } else {
            CharMotionShiftSet(scene->teams[scene->winner].players[0]->charNo,
                               scene->teams[scene->winner].players[0]->motions[2],
                               0.0f, 5.0f, 0U);
            CharMotionShiftSet(scene->teams[scene->winner].players[1]->charNo,
                               scene->teams[scene->winner].players[1]->motions[2],
                               0.0f, 5.0f, 0U);
            temp_r30 = scene->teams[scene->winner].players[0]->playerNo;
            if (_CheckFlag(65551U) == 0) {
                GwPlayer[temp_r30].mgCoinBonus = 10;
            }
            temp_r29 = scene->teams[scene->winner].players[1]->playerNo;
            if (_CheckFlag(65551U) == 0) {
                GwPlayer[temp_r29].mgCoinBonus = 10;
            }
            Hu3DModelObjPosGet(scene->teams[scene->winner].actor.modelA,
                               lbl_1_data_4CC,
                               &scene->teams[scene->winner].players[0]->pos);
            Hu3DModelObjPosGet(scene->teams[scene->winner].actor.modelA,
                               lbl_1_data_4CE,
                               &scene->teams[scene->winner].players[1]->pos);
            scene->teams[scene->winner].players[0]->rot.y = 0.0f;
            scene->teams[scene->winner].players[1]->rot.y = 0.0f;
            Hu3DModelHookObjReset(scene->teams[scene->winner].actor.modelA,
                                  lbl_1_data_4CC);
            Hu3DModelHookObjReset(scene->teams[scene->winner].actor.modelA,
                                  lbl_1_data_4CE);
            fn_1_7554(scene->teams[scene->winner].players[0]);
            fn_1_7554(scene->teams[scene->winner].players[1]);
        }
        scene->field6F8++;
        break;
    case 1:
        break;
    }
}

void fn_1_11EC(s16 mode, s16 frameNo)
{

}

void fn_1_11F0(s16 mode, s16 frameNo)
{
    M638SceneView *scene;

    scene = lbl_1_bss_4->data;
    MgTimerKill(scene->timer);
    espKill(scene->sprites[0]);
    espKill(scene->sprites[1]);
    espKill(scene->sprites[2]);
    espKill(scene->teams[0].sprites[0]);
    espKill(scene->teams[0].sprites[1]);
    espKill(scene->teams[0].sprites[2]);
    espKill(scene->teams[0].sprites[3]);
    espKill(scene->teams[0].sprites[4]);
    espKill(scene->teams[1].sprites[0]);
    espKill(scene->teams[1].sprites[1]);
    espKill(scene->teams[1].sprites[2]);
    espKill(scene->teams[1].sprites[3]);
    espKill(scene->teams[1].sprites[4]);
}

void fn_1_1294(void)
{
    s32 var_r31;

    var_r31 = 0;
    while (var_r31 < 2) {
        Hu3DCameraCreate((s32) lbl_1_rodata_10[var_r31]);
        Hu3DCameraPerspectiveSet((s32) lbl_1_rodata_10[var_r31], 100.0f, 20.0f, 40000.0f, 0.6f);
        Hu3DCameraPosSet((s32) lbl_1_rodata_10[var_r31], 0.0f, -80.0f, 400.0f, 0.0f, 1.0f, 0.0f, 0.0f, -470.0f, -1200.0f);
        var_r31 += 1;
    }
}

void fn_1_13B4(M638LightingView *scene)
{
    Point3d sp8;
    s16 var_r30;

    PSVECSubtract(&scene->lightTarget, &scene->lightPosition, &sp8);
    PSVECNormalize(&sp8, &sp8);
    if (scene->alternateLight != 0) {
        var_r30 = Hu3DGLightCreate(scene->lightPosition.x, scene->lightPosition.y, scene->lightPosition.z, sp8.x, sp8.y, sp8.z, 95U, 95U, 127U);
    } else {
        var_r30 = Hu3DGLightCreate(scene->lightPosition.x, scene->lightPosition.y, scene->lightPosition.z, sp8.x, sp8.y, sp8.z, 207U, 207U, 207U);
    }
    Hu3DGLightInfinitytSet(var_r30);
}

const Point3d lbl_1_rodata_68 = { 0.0f, 1.0f, 0.0f };

void fn_1_1470(M638LightingView *scene)
{
    Point3d sp38;
    Point3d sp2C;
    Point3d sp20;
    Point3d sp14;
    Point3d sp8;

    sp8 = lbl_1_rodata_68;
    sp14.x = scene->lightPosition.x;
    sp14.y = 0.3f * scene->lightPosition.y;
    sp14.z = 0.3f * scene->lightPosition.z;
    PSVECSubtract(&sp14, &scene->lightTarget, &sp2C);
    PSVECNormalize(&sp2C, &sp2C);
    PSVECCrossProduct(&sp8, &sp2C, &sp20);
    PSVECNormalize(&sp20, &sp20);
    PSVECCrossProduct(&sp2C, &sp20, &sp38);
    PSVECNormalize(&sp38, &sp38);
    Hu3DShadowMultiCreate(100.0f, 20.0f, 40000.0f, (s16) (lbl_1_rodata_10[0] | lbl_1_rodata_10[1]));
    Hu3DShadowMultiTPLvlSet(0.5f, (s16) (lbl_1_rodata_10[0] | lbl_1_rodata_10[1]));
    Hu3DShadowMultiPosSet(&sp14, &sp38, &scene->lightTarget, (s16) (lbl_1_rodata_10[0] | lbl_1_rodata_10[1]));
    if (scene->alternateLight != 0) {
        Hu3DShadowMultiColSet(15U, 15U, 95U, (s16) (lbl_1_rodata_10[0] | lbl_1_rodata_10[1]));
        return;
    }
    Hu3DShadowMultiColSet(63U, 63U, 63U, (s16) (lbl_1_rodata_10[0] | lbl_1_rodata_10[1]));
}

void fn_1_165C(M638CameraWork *arg0)
{
    arg0->elapsed = 0.0f;
    arg0->splitX = 320.0f;
    fn_1_16AC(arg0);
}

void fn_1_16AC(M638CameraWork *work)
{
    int camera;
    float aspect;
    camera = 0;
    Hu3DCameraViewportSet(lbl_1_rodata_10[camera], 0.0f, 0.0f,
        16.0f + work->splitX, 480.0f, 0.0f, 1.0f);
    camera++;
    Hu3DCameraViewportSet(lbl_1_rodata_10[camera], work->splitX - 16.0f,
        0.0f, 16.0f + (640.0f - work->splitX), 480.0f, 0.0f, 1.0f);
    camera = 0;
    Hu3DCameraScissorSet(lbl_1_rodata_10[camera], 0, 0, (u32)work->splitX, 480);
    camera++;
    Hu3DCameraScissorSet(lbl_1_rodata_10[camera], (u32)work->splitX, 0,
        (u32)(640.0f - work->splitX), 480);
    camera = 0;
    aspect = (0.9f * work->splitX) / 480.0f;
    if (aspect < 0.6f) {
        aspect = 0.6f;
    }
    Hu3DCameraPerspectiveSet(lbl_1_rodata_10[camera], 100.0f, 20.0f, 40000.0f, aspect);
    camera++;
    aspect = (0.9f * (640.0f - work->splitX)) / 480.0f;
    if (aspect < 0.6f) {
        aspect = 0.6f;
    }
    Hu3DCameraPerspectiveSet(lbl_1_rodata_10[camera], 100.0f, 20.0f, 40000.0f, aspect);
}

void fn_1_1988(OMOBJ *obj)
{
    fn_1_75BC();
    obj->objFunc = fn_1_19C0;
}

void fn_1_19C0(OMOBJ *obj)
{
    M638SceneView *work;
    int camera;
    float aspect;
    work = lbl_1_bss_4->data;
    fn_1_2288(&work->teams[0]);
    fn_1_2288(&work->teams[1]);
    fn_1_4B4C((M638CourseView *)((u8 *)work + 60));
    fn_1_4B4C((M638CourseView *)((u8 *)work + 896));
    fn_1_5378((M638SceneCameraView *)work);
    camera = 0;
    Hu3DCameraViewportSet(lbl_1_rodata_10[camera], 0.0f, 0.0f,
        16.0f + work->camera.splitX, 480.0f, 0.0f, 1.0f);
    camera++;
    Hu3DCameraViewportSet(lbl_1_rodata_10[camera], work->camera.splitX - 16.0f,
        0.0f, 16.0f + (640.0f - work->camera.splitX), 480.0f, 0.0f, 1.0f);
    camera = 0;
    Hu3DCameraScissorSet(lbl_1_rodata_10[camera], 0, 0, (u32)work->camera.splitX, 480);
    camera++;
    Hu3DCameraScissorSet(lbl_1_rodata_10[camera], (u32)work->camera.splitX, 0,
        (u32)(640.0f - work->camera.splitX), 480);
    camera = 0;
    aspect = (0.9f * work->camera.splitX) / 480.0f;
    if (aspect < 0.6f) {
        aspect = 0.6f;
    }
    Hu3DCameraPerspectiveSet(lbl_1_rodata_10[camera], 100.0f, 20.0f, 40000.0f, aspect);
    camera++;
    aspect = (0.9f * (640.0f - work->camera.splitX)) / 480.0f;
    if (aspect < 0.6f) {
        aspect = 0.6f;
    }
    Hu3DCameraPerspectiveSet(lbl_1_rodata_10[camera], 100.0f, 20.0f, 40000.0f, aspect);
    fn_1_7754();
}

void fn_1_1CD4(M638SceneView *scene)
{
    s32 i;
    s16 night;

    scene->teams[0].players[0] = NULL;
    scene->teams[0].players[1] = NULL;
    scene->teams[1].players[0] = NULL;
    scene->teams[1].players[1] = NULL;
    for (i = 0; i < 73; i++) {
        scene->models[i] = -1;
    }
    for (i = 0; i < 1; i++) {
        scene->extraModels[i] = -1;
    }
    night = GwMgNightF;
    scene->alternateLight = night == 1;
    scene->playState = 0;
    scene->introState = 0;
    scene->startState = 0;
    scene->resultState = 0;
    scene->finishState = 0;
    scene->field6F8 = 0;
    scene->winner = -1;
    scene->exitTime = 0.0f;
    scene->newRecord = 0;
    scene->vector0.x = 0.0f;
    scene->vector0.y = 500.0f;
    scene->vector0.z = 1000.0f;
    scene->vector1.x = 0.0f;
    scene->vector1.y = 0.0f;
    scene->vector1.z = 1.0f;
    fn_1_4D68((M638SceneChoicesView *)scene);
}

void fn_1_1E58(M638SceneView *scene)
{
    fn_1_309C(&scene->teams[0].actor, 0);
    fn_1_309C(&scene->teams[1].actor, 1);
    Hu3DModelHookSet(scene->teams[0].actor.modelB, lbl_1_data_4D0, scene->teams[0].actor.modelD);
    Hu3DModelHookSet(scene->teams[0].actor.modelB, lbl_1_data_4D7, scene->teams[0].actor.modelC);
    Hu3DModelHookSet(scene->teams[1].actor.modelB, lbl_1_data_4D0, scene->teams[1].actor.modelD);
    Hu3DModelHookSet(scene->teams[1].actor.modelB, lbl_1_data_4D7, scene->teams[1].actor.modelC);
    fn_1_1F18(&scene->teams[0], 0);
    fn_1_1F18(&scene->teams[1], 1);
    fn_1_5528(&scene->teams[0].scale, 0);
    fn_1_5528(&scene->teams[1].scale, 1);
}

void fn_1_1F18(M638TeamView *team, s32 camera)
{
    s16 model;
    M638SceneView *scene;
    s32 i;

    scene = lbl_1_bss_4->data;
    Hu3DModelHookSet(team->actor.modelA, lbl_1_data_4CC, team->players[0]->model);
    Hu3DModelHookSet(team->actor.modelA, lbl_1_data_4CE, team->players[1]->model);
    team->players[0]->attachedModel = team->actor.modelC;
    team->players[1]->attachedModel = team->actor.modelD;
    team->button = 0;
    team->count = 0;
    team->active = 0;
    team->blend = 0.0f;
    team->field316 = 0;
    team->field318 = 0;
    team->variant = camera;
    team->camera = (u16)lbl_1_rodata_10[camera];
    team->field320 = 0.0f;
    team->field31C = 0.0f;
    for (i = 0; i < 2; i++) {
        if (scene->models[33] < 0) {
            model = scene->models[33] = Hu3DModelCreate(HuDataSelHeapReadNum(lbl_1_data_3A8[33], 268435456, HEAP_MODEL));
        } else {
            model = Hu3DModelLink(scene->models[33]);
        }
        team->props[i].model = model;
        Hu3DModelAttrSet(model, 1073741826U);
        Hu3DModelLayerSet(model, 5);
        Hu3DModelPosSet(model, 0.0f, -120.0f, 125.0f);
        Hu3DModelRotSet(model, 75.0f, 0.0f, 0.0f);
        Hu3DModelAttrReset(model, 1U);
        Hu3DModelCameraSet(model, (u16)lbl_1_rodata_10[camera]);
        Hu3DMotionSpeedSet(model, 0.75f);
        Hu3DModelScaleSet(model, 1.5f, 1.5f, 1.5f);
    }
    if (scene->models[34] < 0) {
        model = scene->models[34] = Hu3DModelCreate(HuDataSelHeapReadNum(lbl_1_data_3A8[34], 268435456, HEAP_MODEL));
    } else {
        model = Hu3DModelLink(scene->models[34]);
    }
    team->props[2].model = model;
    Hu3DModelAttrReset(model, 1073741826U);
    Hu3DModelAttrSet(model, 1073741825U);
    Hu3DModelLayerSet(model, 5);
    team->props[2].pos.x = 0.0f;
    team->props[2].pos.y = -120.0f;
    team->props[2].pos.z = 400.0f;
    Hu3DModelPosSetV(model, &team->props[2].pos);
    Hu3DModelRotSet(model, 75.0f, 0.0f, 0.0f);
    Hu3DModelAttrReset(model, 1U);
    Hu3DModelCameraSet(model, (u16)lbl_1_rodata_10[camera]);
    Hu3DMotionSpeedSet(model, 7.28f);
    team->sound0 = -1;
    team->sound1 = -1;
}

void fn_1_2288(M638TeamView *team)
{
    Point3d sp18;
    s32 sounds[2] = { 1883, 1884 };
    M638Prop *temp_r30;
    M638SceneView *temp_r29;
    f32 var_f31;
    f32 var_f30;
    f32 var_f29;
    f32 temp_f28;

    temp_r29 = lbl_1_bss_4->data;
    if (team->active != 0) {
        team->field330 += 1;
        if (team->field330 > 5) {
            espAttrSet(team->sprites[((team->button * 2) + 1) + team->field334], 4U);
            team->field330 = 0;
            team->field334 ^= 1;
            espAttrReset(team->sprites[((team->button * 2) + 1) + team->field334], 4U);
        }
        fn_1_2CDC(team);
    }
    if (team->actor.mode != 0) {
        if ((team->players[0]->buttonB != 0) || (team->players[1]->buttonB != 0)) {
            team->field316 = 0;
        } else if ((team->players[0]->buttonA != 0) || (team->players[1]->buttonA != 0)) {
            team->field316 = 10;
        } else if (team->field316 > 0) {
            team->field316 -= 1;
        }
        if (team->field316 != 0) {
            team->actor.pos.x += 0.05f;
            if (team->actor.pos.x > 1.0f) {
                team->actor.pos.x = 1.0f;
            }
            var_f31 = -15.0f * team->actor.pos.x;
            team->actor.pos.y = 0.0f;
            if (team->actor.pos.z < 1.0f) {
                team->actor.pos.z += 0.1f;
            }
        } else {
            team->actor.pos.x *= 0.995f;
            if (team->actor.pos.x < 0.005f) {
                team->actor.pos.x = 0.0f;
            }
            team->actor.pos.y += 5.0f;
            while (team->actor.pos.y > 360.0f) {
                team->actor.pos.y -= 360.0f;
            }
            var_f31 = (f32) ((f64) (-15.0f * team->actor.pos.x) * cos((f64) ((team->actor.pos.y * acosf(-1.0f)) / 180.0f)));
            if (team->actor.pos.z > 0.5f) {
                team->actor.pos.z -= 0.1f;
            }
        }
        if (team->actor.rot.x > var_f31) {
            team->actor.rot.x -= team->actor.pos.x;
            if (team->actor.rot.x < var_f31) {
                team->actor.rot.x = var_f31;
            }
        }
        if (team->actor.rot.x < var_f31) {
            team->actor.rot.x += team->actor.pos.x;
            if (team->actor.rot.x > var_f31) {
                team->actor.rot.x = var_f31;
            }
        }
    }
    var_f29 = Hu3DMotionTimeGet(team->course.model);
    if (var_f29 >= lbl_1_data_330[team->actor.state + 1].unk0) {
        team->actor.state += 1;
    }
    temp_f28 = team->actor.pos.z * lbl_1_data_330[team->actor.state].unk4;
    if (team->actor.rot.z < temp_f28) {
        team->actor.rot.z += 0.05f;
    }
    if (team->actor.rot.z > temp_f28) {
        team->actor.rot.z -= 0.05f;
    }
    Hu3DMotionSpeedSet(team->course.alternateModel, team->blend);
    Hu3DMotionSpeedSet(team->course.model, team->blend);
    Hu3DMotionClusterTimeSet(team->course.alternateModel, 0, var_f29);
    if (team->blend > 0.2f) {
        var_f30 = (team->blend - 0.2f) / 0.2f;
        if (var_f30 > 1.0f) {
            var_f30 = 1.0f;
        }
        Hu3DModelAttrReset(team->props[2].model, 1U);
    } else {
        var_f30 = 0.0f;
        Hu3DModelAttrSet(team->props[2].model, 1U);
    }
    team->props[2].pos.z = 800.0f - (400.0f * var_f30);
    Hu3DModelPosSetV(team->props[2].model, &team->props[2].pos);
    fn_1_3518(&team->actor, (s32) team->active, team->blend);
    Hu3DModelRotSetV(team->actor.modelB, &team->actor.rot);
    Hu3DModelRotSetV(team->actor.modelA, &team->actor.rot);
    if (Hu3DMotionTimeGet(team->course.model) >= temp_r29->eventTimes[team->count]) {
        espAttrSet(team->sprites[(team->button * 2) + 1], 4U);
        espAttrSet(team->sprites[(team->button * 2) + 2], 4U);
        team->count += 1;
        team->button = temp_r29->choices[team->count];
        if (team->count < 10) {
            if (team->active != 0) {
                espAttrReset(team->sprites[((team->button * 2) + 1) + team->field334], 4U);
            }
            if (fn_1_7660(fn_1_2ED0, &team->props[team->field318]) != 0) {
                team->props[team->field318].state = 0;
                team->field318 ^= 1;
            }
            temp_r30 = &team->course.pair[team->button][team->course.pairIndex[team->button]];
            if (fn_1_7660(fn_1_2F94, temp_r30) != 0) {
                Hu3DModelObjPosGet(team->course.events[team->count - 1], lbl_1_data_4DE, &sp18);
                temp_r30->pos.x = (f32) (152.22000122070312 * sin((f64) ((lbl_1_data_380[team->count - 1] * acosf(-1.0f)) / 180.0f)));
                temp_r30->pos.y = (f32) (-152.22000122070312 * cos((f64) ((lbl_1_data_380[team->count - 1] * acosf(-1.0f)) / 180.0f)));
                temp_r30->pos.z = sp18.z;
                Hu3DModelAttrSet(team->course.overlays[team->count - 1], 1U);
                Hu3DModelRotSet(temp_r30->model, 0.0f, 0.0f, lbl_1_data_380[team->count - 1]);
                temp_r30->state = 0;
                team->course.pairIndex[team->button] ^= 1;
                if (team->variant == 0) {
                    if (team->button != 0) {
                        HuAudFXPlay(1887);
                    } else {
                        HuAudFXPlay(1889);
                    }
                } else if (team->button != 0) {
                    HuAudFXPlay(1888);
                } else {
                    HuAudFXPlay(1890);
                }
            }
        }
        if (fn_1_7660(fn_1_368C, &team->actor) != 0) {
            team->actor.count = 0;
        }
    }
    if (team->field31C < (team->blend - 0.1f)) {
        team->field31C += 0.005f;
        if (team->field31C > 0.4f) {
            team->field31C = 0.4f;
        }
    } else if (team->field31C > team->blend) {
        team->field31C -= 0.01f;
        if (team->field31C < 0.0f) {
            team->field31C = 0.0f;
        }
    }
    Hu3DCameraPosSet((s32) team->camera, 0.0f, -80.0f, 400.0f + (100.0f * team->field31C), 0.0f, 1.0f, 0.0f, 0.0f, -470.0f, -1200.0f);
    fn_1_56B4(&team->scale);
    if (team->blend > 0.0f) {
        if (team->sound0 == -1) {
            team->sound0 = HuAudFXPlay((&sounds[0])[team->variant]);
            return;
        }
    } else if (team->sound0 > -1) {
        HuAudFXStop(team->sound0);
        team->sound0 = -1;
    }
}

void fn_1_2CDC(M638TeamView *team)
{
    f32 var_f30;
    f32 var_f31;
    s32 i;

    var_f31 = 0.0f;
    var_f30 = 0.0f;
    if (team->players[0]->buttonB != 0) {
        team->players[1]->impulse *= 0.8f;
    }
    if (team->players[1]->buttonB != 0) {
        team->players[0]->impulse *= 0.8f;
    }
    for (i = 0; i < 2; i++) {
        if (team->players[i]->impulse > 10.0f) {
            var_f31 += 0.5f;
            var_f30 += 0.05f * (team->players[i]->impulse - 10.0f);
        } else {
            var_f31 += 0.05f * team->players[i]->impulse;
        }
    }
    team->blend = (0.3f * var_f31) + (0.1f * var_f30);
    if (((team->players[0]->buttonB != 0) ||
         (team->players[1]->buttonB != 0)) &&
        (team->blend > 0.0f)) {
        fn_1_5760(&team->scale, team->blend / 0.4f);
        if (team->variant == 0) {
            HuAudFXPlay(1885);
            return;
        }
        HuAudFXPlay(1886);
    }
}

s32 fn_1_2ED0(void *data)
{
    M638Prop *prop;
    int done;
    done = 0;
    prop = data;
    switch (prop->state) {
    case 0:
        Hu3DModelAttrReset(prop->model, 1U);
        Hu3DModelAttrReset(prop->model, 1073741826U);
        Hu3DMotionTimeSet(prop->model, 0.0f);
        prop->state++;
        /* fallthrough */
    case 1:
        if (Hu3DMotionEndCheck(prop->model)) {
            Hu3DModelAttrSet(prop->model, 1U);
            Hu3DModelAttrSet(prop->model, 1073741826U);
            done = 1;
        }
        break;
    }
    return done;
}

s32 fn_1_2F94(void *data)
{
    M638Prop *prop;
    int done;
    done = 0;
    prop = data;
    switch (prop->state) {
    case 0:
        Hu3DModelAttrReset(prop->model, 1U);
        Hu3DModelAttrReset(prop->model, 1073741826U);
        Hu3DModelAttrReset(prop->model, 3221225474U);
        Hu3DMotionTimeSet(prop->model, 0.0f);
        Hu3DMotionClusterTimeSet(prop->model, 0, 0.0f);
        Hu3DModelPosSetV(prop->model, &prop->pos);
        prop->state++;
        /* fallthrough */
    case 1:
        if (Hu3DMotionEndCheck(prop->model)) {
            Hu3DModelAttrSet(prop->model, 1U);
            Hu3DModelAttrSet(prop->model, 1073741826U);
            Hu3DModelAttrSet(prop->model, 3221225474U);
            done = 1;
        }
        break;
    }
    return done;
}

void fn_1_309C(M638TeamActorView *work, s32 camera)
{
    s16 model;
    M638ModelCacheView *scene;
    scene = lbl_1_bss_4->data;
    work->rot.x = 0.0f;
    work->rot.y = 0.0f;
    work->rot.z = 0.0f;
    work->pos.x = 0.0f;
    work->pos.y = 0.0f;
    work->pos.z = 0.0f;
    work->state = 0;
    work->blinkCount = 0;
    work->count = 0;
    work->blend = 0.0f;
    work->mode = 1;
    if (scene->models[camera + 29] < 0) {
        model = scene->models[camera + 29] = Hu3DModelCreate(HuDataSelHeapReadNum(lbl_1_data_3A8[camera + 29], 268435456, HEAP_MODEL));
    } else {
        model = Hu3DModelLink(scene->models[camera + 29]);
    }
    Hu3DModelAttrSet(model, 1073741826U);
    Hu3DModelLayerSet(model, 3);
    Hu3DModelPosSet(model, 0.0f, 0.0f, 0.0f);
    Hu3DModelRotSetV(model, &work->rot);
    Hu3DModelAttrReset(model, 1U);
    Hu3DModelCameraSet(model, (u16)lbl_1_rodata_10[camera]);
    Hu3DModelShadowMapObjSet(model, lbl_1_data_4E4);
    work->modelB = model;
    if (scene->models[28] < 0) {
        model = scene->models[28] = Hu3DModelCreate(HuDataSelHeapReadNum(lbl_1_data_3A8[28], 268435456, HEAP_MODEL));
    } else {
        model = Hu3DModelLink(scene->models[28]);
    }
    Hu3DModelAttrSet(model, 1073741826U);
    Hu3DModelLayerSet(model, 3);
    Hu3DModelPosSet(model, 0.0f, 0.0f, 0.0f);
    Hu3DModelRotSetV(model, &work->rot);
    Hu3DModelAttrReset(model, 1U);
    Hu3DModelCameraSet(model, (u16)lbl_1_rodata_10[camera]);
    Hu3DModelShadowSet(model);
    work->modelA = model;
    if (scene->models[31] < 0) {
        model = scene->models[31] = Hu3DModelCreate(HuDataSelHeapReadNum(lbl_1_data_3A8[31], 268435456, HEAP_MODEL));
    } else {
        model = Hu3DModelLink(scene->models[31]);
    }
    Hu3DModelAttrSet(model, 1073741826U);
    Hu3DModelLayerSet(model, 3);
    Hu3DModelPosSet(model, 0.0f, 0.0f, 0.0f);
    Hu3DModelRotSet(model, 0.0f, 0.0f, 0.0f);
    Hu3DModelAttrReset(model, 1U);
    Hu3DModelCameraSet(model, (u16)lbl_1_rodata_10[camera]);
    work->modelC = model;
    if (scene->models[31] < 0) {
        model = scene->models[31] = Hu3DModelCreate(HuDataSelHeapReadNum(lbl_1_data_3A8[31], 268435456, HEAP_MODEL));
    } else {
        model = Hu3DModelLink(scene->models[31]);
    }
    Hu3DModelAttrSet(model, 1073741826U);
    Hu3DModelLayerSet(model, 3);
    Hu3DModelPosSet(model, 0.0f, 0.0f, 0.0f);
    Hu3DModelRotSet(model, 0.0f, 0.0f, 0.0f);
    Hu3DModelAttrReset(model, 1U);
    Hu3DModelCameraSet(model, (u16)lbl_1_rodata_10[camera]);
    work->modelD = model;
}

void fn_1_3518(M638TeamActorView *work, s32 mode, f32 value)
{
    f32 var_f31;

    if (mode != 0) {
        var_f31 = value / 0.3f;
        if (var_f31 > 1.0f) {
            var_f31 = 1.0f;
        }
    } else if (value > 0.0f) {
        work->blinkCount = (s32) (work->blinkCount + 1);
        work->blinkCount = (s32) (work->blinkCount % 47);
        if ((s32) work->blinkCount < 3) {
            var_f31 = (f32) work->blinkCount / 3.0f;
        } else if ((s32) work->blinkCount < 31) {
            var_f31 = 1.0f;
        } else {
            var_f31 = 0.0f;
        }
    } else {
        var_f31 = 0.0f;
    }
    Hu3DMotionTimeSet(work->modelB, 16.0f * var_f31);
}

s32 fn_1_368C(void *data)
{
    M638BounceView *work;
    f32 angle;
    f32 height;
    int done;
    done = 0;
    work = data;
    work->count++;
    angle = 36.0f * work->count;
    if (angle > 180.0f) {
        angle -= 180.0f;
    }
    {
        f32 pi = acos(-1.0);
        height = 4.0 * sin((angle * pi) / 180.0f);
    }
    if (work->count >= 20) {
        height = 0.0f;
        done = 1;
    }
    Hu3DModelPosSet(work->modelB, 0.0f, height, 0.0f);
    Hu3DModelPosSet(work->modelA, 0.0f, height, 0.0f);
    return done;
}

void fn_1_3804(void *arg0)
{
    Hu3DReflectMapSet(HuDataSelHeapReadNum(6357044, 268435456, HEAP_MODEL));
    fn_1_386C((void *) ((u8 *) arg0 + 60), 0);
    fn_1_386C((void *) ((u8 *) arg0 + 896), 1);
    fn_1_4948((void *) ((u8 *) arg0 + 60));
    fn_1_4948((void *) ((u8 *) arg0 + 896));
}

void fn_1_386C(M638CourseView *course, s32 camera)
{
    s16 model;
    M638SceneCourseView *scene;
    s32 i;
    s32 j;
    s32 variant;

    scene = lbl_1_bss_4->data;
    if (scene->models[21] < 0) {
        model = scene->models[21] = Hu3DModelCreate(HuDataSelHeapReadNum(lbl_1_data_3A8[21], 268435456, HEAP_MODEL));
    } else {
        model = Hu3DModelLink(scene->models[21]);
    }
    course->model = model;
    Hu3DModelAttrReset(model, 1073741826U);
    Hu3DModelLayerSet(model, 0);
    Hu3DModelPosSet(model, 0.0f, 0.0f, 0.0f);
    Hu3DModelRotSet(model, 0.0f, 0.0f, 0.0f);
    Hu3DModelCameraSet(model, (u16) lbl_1_rodata_10[camera]);
    Hu3DMotionSpeedSet(model, 0.0f);
    Hu3DModelAttrReset(model, 1U);
    if (scene->alternateLight != 0) {
        if (scene->models[70] < 0) {
            model = scene->models[70] = Hu3DModelCreate(HuDataSelHeapReadNum(lbl_1_data_3A8[70], 268435456, HEAP_MODEL));
        } else {
            model = Hu3DModelLink(scene->models[70]);
        }
    } else if (scene->models[24] < 0) {
        model = scene->models[24] = Hu3DModelCreate(HuDataSelHeapReadNum(lbl_1_data_3A8[24], 268435456, HEAP_MODEL));
    } else {
        model = Hu3DModelLink(scene->models[24]);
    }
    course->alternateModel = model;
    Hu3DModelAttrReset(model, 1073741826U);
    Hu3DModelLayerSet(model, 0);
    Hu3DModelPosSet(model, 0.0f, 0.0f, 0.0f);
    Hu3DModelRotSet(model, 0.0f, 0.0f, 0.0f);
    Hu3DModelCameraSet(model, (u16) lbl_1_rodata_10[camera]);
    Hu3DMotionSpeedSet(model, 0.0f);
    Hu3DModelAttrReset(model, 1U);
    Hu3DMotionClusterTimeSet(model, 0, 0.0f);
    Hu3DModelAttrReset(model, 3221225474U);
    i = 0;
    while (i < 4) {
        if (scene->alternateLight != 0) {
            if (scene->models[i + 49] < 0) {
                model = scene->models[i + 49] = Hu3DModelCreate(HuDataSelHeapReadNum(lbl_1_data_3A8[i + 49], 268435456, HEAP_MODEL));
            } else {
                model = Hu3DModelLink(scene->models[i + 49]);
            }
        } else if (scene->models[i] < 0) {
            model = scene->models[i] = Hu3DModelCreate(HuDataSelHeapReadNum(lbl_1_data_3A8[i], 268435456, HEAP_MODEL));
        } else {
            model = Hu3DModelLink(scene->models[i]);
        }
        course->segments[i] = model;
        Hu3DModelAttrSet(model, 1073741826U);
        Hu3DModelAttrReset(model, 1U);
        Hu3DModelLayerSet(model, 1);
        Hu3DModelPosSet(model, 0.0f, 0.0f, 0.0f);
        Hu3DModelRotSet(model, 0.0f, 0.0f, 0.0f);
        Hu3DModelCameraSet(model, (u16) lbl_1_rodata_10[camera]);
        Hu3DMotionSpeedSet(model, 0.2f);
        i += 1;
    }
    while (i < 20) {
        if (scene->alternateLight != 0) {
            if (scene->models[i + 49] < 0) {
                model = scene->models[i + 49] = Hu3DModelCreate(HuDataSelHeapReadNum(lbl_1_data_3A8[i + 49], 268435456, HEAP_MODEL));
            } else {
                model = Hu3DModelLink(scene->models[i + 49]);
            }
        } else if (scene->models[i] < 0) {
            model = scene->models[i] = Hu3DModelCreate(HuDataSelHeapReadNum(lbl_1_data_3A8[i], 268435456, HEAP_MODEL));
        } else {
            model = Hu3DModelLink(scene->models[i]);
        }
        course->segments[i] = model;
        Hu3DModelAttrSet(model, 1073741826U);
        Hu3DModelAttrSet(model, 1U);
        Hu3DModelLayerSet(model, 1);
        Hu3DModelPosSet(model, 0.0f, 0.0f, 0.0f);
        Hu3DModelRotSet(model, 0.0f, 0.0f, 0.0f);
        Hu3DModelCameraSet(model, (u16) lbl_1_rodata_10[camera]);
        if (i == 4) {
            Hu3DModelAttrReset(model, 1073741826U);
            Hu3DModelAttrSet(model, 1073741825U);
            Hu3DMotionSpeedSet(model, 0.2f);
        }
        if (i == 5) {
            Hu3DModelAttrReset(model, 1073741826U);
            Hu3DModelAttrSet(model, 1073741825U);
            Hu3DMotionSpeedSet(model, 0.5f);
        }
        if (i == 16) {
            Hu3DModelAttrReset(model, 1073741826U);
            Hu3DModelAttrSet(model, 1073741825U);
            Hu3DMotionSpeedSet(model, 0.3f);
        }
        if (i == 17) {
            Hu3DModelAttrReset(model, 1073741826U);
            Hu3DModelAttrSet(model, 1073741825U);
            Hu3DMotionSpeedSet(model, 0.3f);
        }
        i += 1;
    }
    if (scene->alternateLight != 0) {
        if (scene->models[69] < 0) {
            model = Hu3DModelCreate(HuDataSelHeapReadNum(lbl_1_data_3A8[69], 268435456, HEAP_MODEL));
            scene->models[69] = model;
        } else {
            model = Hu3DModelLink(scene->models[69]);
        }
    } else if (scene->models[20] < 0) {
        model = scene->models[20] = Hu3DModelCreate(HuDataSelHeapReadNum(lbl_1_data_3A8[20], 268435456, HEAP_MODEL));
    } else {
        model = Hu3DModelLink(scene->models[20]);
    }
    course->lastModel = model;
    Hu3DModelAttrSet(model, 1073741826U);
    Hu3DModelLayerSet(model, 1);
    Hu3DModelPosSet(model, 0.0f, 0.0f, 0.0f);
    Hu3DModelRotSet(model, 0.0f, 0.0f, 0.0f);
    Hu3DModelCameraSet(model, (u16) lbl_1_rodata_10[camera]);
    Hu3DModelAttrSet(model, 1U);
    j = 1;
    variant = 0;
    i = 0;
    while (i < 40) {
        if (scene->models[variant + 22] < 0) {
            model = scene->models[variant + 22] = Hu3DModelCreate(HuDataSelHeapReadNum(lbl_1_data_3A8[variant + 22], 268435456, HEAP_MODEL));
        } else {
            model = Hu3DModelLink(scene->models[variant + 22]);
        }
        course->connectors[i] = model;
        Hu3DModelAttrSet(model, 1073741826U);
        Hu3DModelLayerSet(model, 1);
        Hu3DModelPosSet(model, 0.0f, 0.0f, 0.0f);
        Hu3DModelRotSet(model, 0.0f, 0.0f, 0.0f);
        Hu3DModelAttrReset(model, 1U);
        Hu3DModelCameraSet(model, (u16) lbl_1_rodata_10[camera]);
        if (strcmp(lbl_1_data_290[i], lbl_1_data_230) != 0) {
            if (j < 10) {
                variant = scene->choices[j];
                if (scene->models[variant + 25] < 0) {
                    model = scene->models[variant + 25] = Hu3DModelCreate(HuDataSelHeapReadNum(lbl_1_data_3A8[variant + 25], 268435456, HEAP_MODEL));
                } else {
                    model = Hu3DModelLink(scene->models[variant + 25]);
                }
                course->events[j - 1] = model;
                Hu3DModelAttrSet(model, 1073741826U);
                Hu3DModelLayerSet(model, 1);
                Hu3DModelPosSet(model, 0.0f, 0.0f, 0.0f);
                Hu3DModelRotSet(model, 0.0f, 0.0f, 0.0f);
                Hu3DModelAttrReset(model, 1U);
                Hu3DModelCameraSet(model, (u16) lbl_1_rodata_10[camera]);
                scene->eventTimes[j - 1] = 15.0f + (15.0f * (f32) i);
                if (scene->models[variant + 39] < 0) {
                    model = Hu3DModelCreate(HuDataSelHeapReadNum(lbl_1_data_3A8[variant + 39], 268435456, HEAP_MODEL));
                    scene->models[variant + 39] = model;
                } else {
                    model = Hu3DModelLink(scene->models[variant + 39]);
                }
                course->overlays[j - 1] = model;
                Hu3DModelAttrSet(model, 1073741826U);
                Hu3DModelLayerSet(model, 5);
                Hu3DModelPosSet(model, 0.0f, 0.0f, 0.0f);
                Hu3DModelRotSet(model, 0.0f, 0.0f, 0.0f);
                Hu3DModelAttrReset(model, 1U);
                Hu3DModelCameraSet(model, (u16) lbl_1_rodata_10[camera]);
            } else {
                variant = scene->choices[j];
                if (scene->models[27] < 0) {
                    model = scene->models[27] = Hu3DModelCreate(HuDataSelHeapReadNum(lbl_1_data_3A8[27], 268435456, HEAP_MODEL));
                } else {
                    model = Hu3DModelLink(scene->models[27]);
                }
                course->events[j - 1] = model;
                Hu3DModelAttrSet(model, 1073741826U);
                Hu3DModelLayerSet(model, 1);
                Hu3DModelPosSet(model, 0.0f, 0.0f, 0.0f);
                Hu3DModelRotSet(model, 0.0f, 0.0f, 0.0f);
                Hu3DModelAttrReset(model, 1U);
                Hu3DModelCameraSet(model, (u16) lbl_1_rodata_10[camera]);
                scene->eventTimes[j - 1] = 15.0f + (15.0f * (f32) i);
            }
            j += 1;
        }
        i += 1;
    }
    scene->endTime = 601.0f;
    if (scene->alternateLight != 0) {
        i = 0;
        while (i < 5) {
            if (scene->models[71] < 0) {
                model = scene->models[71] = Hu3DModelCreate(HuDataSelHeapReadNum(lbl_1_data_3A8[71], 268435456, HEAP_MODEL));
            } else {
                model = Hu3DModelLink(scene->models[71]);
            }
            course->nightModels[i] = model;
            Hu3DModelAttrReset(model, 1073741826U);
            Hu3DModelAttrSet(model, 1073741825U);
            Hu3DModelLayerSet(model, 1);
            Hu3DModelPosSet(model, 0.0f, 0.0f, 0.0f);
            Hu3DModelRotSet(model, 0.0f, 0.0f, 0.0f);
            Hu3DModelAttrReset(model, 1U);
            Hu3DModelCameraSet(model, (u16) lbl_1_rodata_10[camera]);
            Hu3DMotionSpeedSet(model, 0.2f);
            i += 1;
        }
    }
    Hu3DModelAttrSet(course->events[0], 1U);
    fn_1_5814(&course->props, camera);
    course->current = 0;
    course->pairIndex[0] = 0;
    course->pairIndex[1] = 0;
    i = 0;
    while (i < 2) {
        j = 0;
        while (j < 2) {
            if (scene->models[41 + camera * 4 + i * 2 + j] < 0) {
                model = scene->models[41 + camera * 4 + i * 2 + j] = Hu3DModelCreate(HuDataSelHeapReadNum(lbl_1_data_3A8[41 + camera * 4 + i * 2 + j], 268435456, HEAP_MODEL));
            } else {
                model = Hu3DModelLink(scene->models[41 + camera * 4 + i * 2 + j]);
            }
            course->pair[i][j].model = model;
            Hu3DModelAttrSet(model, 1073741826U);
            Hu3DModelAttrSet(model, 3221225474U);
            Hu3DModelLayerSet(model, 5);
            Hu3DModelPosSet(model, 0.0f, 0.0f, 0.0f);
            Hu3DModelRotSet(model, 0.0f, 0.0f, 0.0f);
            Hu3DModelAttrSet(model, 1U);
            Hu3DModelCameraSet(model, (u16) lbl_1_rodata_10[camera]);
            j += 1;
        }
        i += 1;
    }
}

void fn_1_4948(M638CourseView *course)
{
    M638SceneCourseView *scene;
    s32 i;
    s32 eventCount;

    scene = lbl_1_bss_4->data;
    eventCount = 1;
    for (i = 0; i < 40; i++) {
        Hu3DModelHookSet(course->segments[i / 2], lbl_1_data_190[i], course->connectors[i]);
        if (strcmp(lbl_1_data_290[i], lbl_1_data_230) != 0) {
            Hu3DModelHookSet(course->segments[i / 2], lbl_1_data_290[i], course->events[eventCount - 1]);
            if (eventCount < 10) {
                Hu3DModelHookSet(course->events[eventCount - 1], lbl_1_data_4DE, course->overlays[eventCount - 1]);
            }
            eventCount += 1;
        }
    }
    Hu3DModelHookSet(course->model, lbl_1_data_4EB, course->segments[0]);
    Hu3DModelHookSet(course->model, lbl_1_data_4EF, course->segments[1]);
    Hu3DModelHookSet(course->model, lbl_1_data_4F3, course->segments[2]);
    Hu3DModelHookSet(course->model, lbl_1_data_4F7, course->segments[3]);
    if (scene->alternateLight != 0) {
        Hu3DModelHookSet(course->lastModel, lbl_1_data_4FB, course->nightModels[0]);
        Hu3DModelHookSet(course->lastModel, lbl_1_data_502, course->nightModels[1]);
        Hu3DModelHookSet(course->lastModel, lbl_1_data_509, course->nightModels[2]);
        Hu3DModelHookSet(course->lastModel, lbl_1_data_510, course->nightModels[3]);
        Hu3DModelHookSet(course->lastModel, lbl_1_data_517, course->nightModels[4]);
    }
}

void fn_1_4B4C(M638CourseView *course)
{
    char *hooks[4] = {lbl_1_data_4EB, lbl_1_data_4EF, lbl_1_data_4F3, lbl_1_data_4F7};
    f32 time;
    int hook;
    time = Hu3DMotionTimeGet(course->model);
    if (time > 60.0f + 30.0f * course->current) {
        hook = course->current % 4;
        Hu3DModelHookObjReset(course->model, hooks[hook]);
        Hu3DModelAttrSet(course->segments[course->current], 1U);
        if (course->current + 4 < 20) {
            Hu3DModelHookSet(course->model, hooks[hook], course->segments[course->current + 4]);
            Hu3DModelAttrReset(course->segments[course->current + 4], 1U);
        }
        course->current++;
    }
    if (course->current == 0 && time >= 30.0f && (Hu3DModelAttrGet(course->events[0]) & 1)) {
        Hu3DModelAttrReset(course->events[0], 1U);
    }
    if (time >= 536.0f) {
        if (Hu3DModelAttrGet(course->lastModel) & 1) {
            Hu3DModelAttrReset(course->lastModel, 1U);
            Hu3DModelAttrReset(course->lastModel, 1073741826U);
        } else {
            Hu3DMotionTimeSet(course->lastModel, time - 536.0f);
        }
    }
    fn_1_5D0C(&course->props, time);
}

void fn_1_4D68(M638SceneChoicesView *scene)
{
    int i;
    int repeat;
    repeat = 0;
    scene->choices[0] = 0;
    scene->choices[1] = 0;
    for (i = 2; i < 10; i++) {
        if (repeat) {
            scene->choices[i] = scene->choices[i - 1] ^ 1;
            repeat = 0;
        } else {
            scene->choices[i] = frandmod(2);
            repeat = scene->choices[i] == scene->choices[i - 1];
        }
    }
    scene->choices[10] = scene->choices[9];
}

void fn_1_4E48(M638SceneView *scene)
{
    s16 model;

    model = espEntry(6357041U, 111, 0);
    scene->sprites[0] = model;
    espDrawNoSet(model, 0);
    espPosSet(model, 288.0f, 240.0f);
    espAttrSet(model, 1U);
    espAttrSet(model, 4U);
    model = espEntry(6357042U, 110, 0);
    scene->sprites[1] = model;
    espDrawNoSet(model, 0);
    espPosSet(model, 288.0f, 240.0f);
    espAttrSet(model, 1U);
    espAttrSet(model, 4U);
    model = espEntry(6357043U, 110, 0);
    scene->sprites[2] = model;
    espDrawNoSet(model, 0);
    espPosSet(model, 288.0f, 240.0f);
    espAttrSet(model, 1U);
    espAttrSet(model, 4U);

    model = espEntry(6357073U, 111, 0);
    scene->teams[0].sprites[0] = model;
    espDrawNoSet(model, 0);
    espPosSet(model, 50.0f, 150.0f);
    espAttrSet(model, 1U);
    espAttrSet(model, 4U);
    model = espEntry(6357069U, 110, 0);
    scene->teams[0].sprites[1] = model;
    espDrawNoSet(model, 0);
    espPosSet(model, 50.0f, 150.0f);
    espAttrSet(model, 1U);
    espAttrSet(model, 4U);
    model = espEntry(6357070U, 110, 0);
    scene->teams[0].sprites[2] = model;
    espDrawNoSet(model, 0);
    espPosSet(model, 50.0f, 150.0f);
    espAttrSet(model, 1U);
    espAttrSet(model, 4U);
    model = espEntry(6357071U, 110, 0);
    scene->teams[0].sprites[3] = model;
    espDrawNoSet(model, 0);
    espPosSet(model, 50.0f, 150.0f);
    espAttrSet(model, 1U);
    espAttrSet(model, 4U);
    model = espEntry(6357072U, 110, 0);
    scene->teams[0].sprites[4] = model;
    espDrawNoSet(model, 0);
    espPosSet(model, 50.0f, 150.0f);
    espAttrSet(model, 1U);
    espAttrSet(model, 4U);
    scene->teams[0].field330 = 0;
    scene->teams[0].field334 = 0;

    model = espEntry(6357073U, 111, 0);
    scene->teams[1].sprites[0] = model;
    espDrawNoSet(model, 0);
    espPosSet(model, 526.0f, 150.0f);
    espAttrSet(model, 1U);
    espAttrSet(model, 4U);
    model = espEntry(6357069U, 110, 0);
    scene->teams[1].sprites[1] = model;
    espDrawNoSet(model, 0);
    espPosSet(model, 526.0f, 150.0f);
    espAttrSet(model, 1U);
    espAttrSet(model, 4U);
    model = espEntry(6357070U, 110, 0);
    scene->teams[1].sprites[2] = model;
    espDrawNoSet(model, 0);
    espPosSet(model, 526.0f, 150.0f);
    espAttrSet(model, 1U);
    espAttrSet(model, 4U);
    model = espEntry(6357071U, 110, 0);
    scene->teams[1].sprites[3] = model;
    espDrawNoSet(model, 0);
    espPosSet(model, 526.0f, 150.0f);
    espAttrSet(model, 1U);
    espAttrSet(model, 4U);
    model = espEntry(6357072U, 110, 0);
    scene->teams[1].sprites[4] = model;
    espDrawNoSet(model, 0);
    espPosSet(model, 526.0f, 150.0f);
    espAttrSet(model, 1U);
    espAttrSet(model, 4U);
    scene->teams[1].field330 = 0;
    scene->teams[1].field334 = 0;
}

void fn_1_5378(M638SceneCameraView *scene)
{
    f32 time;
    f32 y;
    time = (Hu3DMotionTimeGet(scene->team0Model) - 120.0f) / 435.0f;
    if (time < 0.0f) {
        time = 0.0f;
    }
    if (time > 1.0f) {
        time = 1.0f;
    }
    y = 400.0f * (1.0f - time) + 120.0f * time;
    espPosSet(scene->sprite0, 276.0f, y);
    time = (Hu3DMotionTimeGet(scene->team1Model) - 120.0f) / 435.0f;
    if (time < 0.0f) {
        time = 0.0f;
    }
    if (time > 1.0f) {
        time = 1.0f;
    }
    y = 400.0f * (1.0f - time) + 120.0f * time;
    espPosSet(scene->sprite1, 300.0f, y);
}

void fn_1_5528(M638ScaleWork *work, s32 camera)
{
    s16 model;
    M638ModelCacheView *scene;
    scene = lbl_1_bss_4->data;
    work->active = 0;
    work->value = 1.0f;
    if (scene->models[32] < 0) {
        model = scene->models[32] = Hu3DModelCreate(HuDataSelHeapReadNum(lbl_1_data_3A8[32], 268435456, HEAP_MODEL));
    } else {
        model = Hu3DModelLink(scene->models[32]);
    }
    work->model = model;
    Hu3DModelAttrSet(model, 1073741826U);
    Hu3DModelLayerSet(model, 1);
    Hu3DModelPosSet(model, 0.0f, 0.0f, 0.0f);
    Hu3DModelRotSet(model, 0.0f, 0.0f, 0.0f);
    Hu3DModelCameraSet(model, (u16)lbl_1_rodata_10[camera]);
    Hu3DModelAttrSet(model, 1U);
    Hu3DMotionSpeedSet(model, 9.74f);
    Hu3DModelScaleSet(model, 1.75f, 1.75f, 1.75f);
}

void fn_1_56B4(M638ScaleWork *work)
{
    f32 temp_f31;

    if (work->active != 0) {
        temp_f31 = 1.0f + ((work->value * Hu3DMotionTimeGet(work->model)) / 60.0f);
        Hu3DModelScaleSet(work->model, temp_f31, temp_f31, temp_f31);
        if (Hu3DMotionEndCheck(work->model) != 0) {
            Hu3DModelAttrSet(work->model, 1U);
            work->active = 0;
        }
    }
}

void fn_1_5760(M638ScaleWork *work, f32 value)
{
    if (work->active == 0) {
        work->value = 1.5f * value;
        Hu3DModelScaleSet(work->model, 1.0f, 1.0f, 1.0f);
        Hu3DModelAttrReset(work->model, 1073741826U);
        Hu3DModelAttrReset(work->model, 1U);
        Hu3DMotionTimeSet(work->model, 0.0f);
        work->active = 1;
    }
}

void fn_1_5814(M638PropGroups *data, s32 camera)
{
    int model_groups[6] = { 0, 1, 1, 0, 0, 1 };
    int i;
    s16 model;
    M638ScenePropsView *scene;
    scene = lbl_1_bss_4->data;
    for (i = 0; i < 6; i++) {
        if (scene->models[model_groups[i] + 35] < 0) {
            model = scene->models[model_groups[i] + 35] = Hu3DModelCreate(HuDataSelHeapReadNum(lbl_1_data_3A8[model_groups[i] + 35], 268435456, HEAP_MODEL));
        } else {
            model = Hu3DModelLink(scene->models[model_groups[i] + 35]);
        }
        data->small[i].model = model;
        Hu3DModelAttrSet(model, 1073741826U);
        Hu3DModelLayerSet(model, 5);
        data->small[i].pos.x = 0.0f;
        data->small[i].pos.y = -100.0f;
        data->small[i].pos.z = 100.0f;
        Hu3DModelPosSetV(model, &data->small[i].pos);
        Hu3DModelRotSet(model, 0.0f, 0.0f, 0.0f);
        Hu3DModelCameraSet(model, (u16)lbl_1_rodata_10[camera]);
        Hu3DModelAttrSet(model, 1U);
        data->small[i].state = 2;
        data->small[i].moving = 0;
    }
    for (i = 0; i < 8; i++) {
        if (scene->models[37] < 0) {
            model = scene->models[37] = Hu3DModelCreate(HuDataSelHeapReadNum(lbl_1_data_3A8[37], 268435456, HEAP_MODEL));
        } else {
            model = Hu3DModelLink(scene->models[37]);
        }
        data->middle[i].model = model;
        Hu3DModelAttrSet(model, 1073741826U);
        Hu3DModelLayerSet(model, 5);
        data->middle[i].pos.x = 0.0f;
        data->middle[i].pos.y = -150.0f;
        data->middle[i].pos.z = 100.0f;
        Hu3DModelPosSetV(model, &data->middle[i].pos);
        Hu3DModelRotSet(model, 0.0f, 0.0f, 0.0f);
        Hu3DModelCameraSet(model, (u16)lbl_1_rodata_10[camera]);
        Hu3DModelAttrSet(model, 1U);
        data->middle[i].state = 2;
        data->middle[i].moving = 0;
    }
    if (scene->alternateLight) {
        for (i = 0; i < 4; i++) {
            if (scene->models[38] < 0) {
                model = scene->models[38] = Hu3DModelCreate(HuDataSelHeapReadNum(lbl_1_data_3A8[38], 268435456, HEAP_MODEL));
            } else {
                model = Hu3DModelLink(scene->models[38]);
            }
            data->night[i].model = model;
            Hu3DModelAttrSet(model, 1073741826U);
            Hu3DModelLayerSet(model, 5);
            data->night[i].pos.x = 0.0f;
            data->night[i].pos.y = -150.0f;
            data->night[i].pos.z = 100.0f;
            Hu3DModelPosSetV(model, &data->night[i].pos);
            Hu3DModelRotSet(model, 0.0f, 0.0f, 0.0f);
            Hu3DModelCameraSet(model, (u16)lbl_1_rodata_10[camera]);
            Hu3DModelAttrSet(model, 1U);
            data->night[i].state = 2;
            data->night[i].moving = 0;
        }
    }
    data->count = 0;
    if (scene->alternateLight) {
        data->state = 0;
    } else {
        data->state = 3;
    }
}

void fn_1_5D0C(M638PropGroups *group, f32 time)
{
    int i;
    switch (group->state) {
    case 0:
        if (time >= 180.0f) {
            group->state++;
        }
        break;
    case 1:
        group->count = 150;
        group->state++;
        /* fallthrough */
    case 2:
        group->count++;
        if (group->count >= 150) {
            for (i = 0; i < 4; i++) {
                if (group->night[i].state == 2 && fn_1_7660(fn_1_6098, &group->night[i]) != 0) {
                    group->night[i].state = 0;
                    Hu3DMotionSpeedSet(group->night[i].model, 0.11f);
                    group->count = 0;
                    break;
                }
            }
        }
        if (time >= 340.0f) {
            group->state++;
        }
        break;
    case 3:
        if (time >= 345.0f) {
            group->state++;
        }
        break;
    case 4:
        group->count = 60;
        group->state++;
        /* fallthrough */
    case 5:
        group->count++;
        if (group->count > 30) {
            for (i = 0; i < 6; i++) {
                if (group->small[i].state == 2 && fn_1_7660(fn_1_6098, &group->small[i]) != 0) {
                    group->small[i].state = 0;
                    Hu3DMotionSpeedSet(group->small[i].model, 0.68f);
                    group->count = 0;
                    break;
                }
            }
        }
        if (time >= 455.0f) {
            group->state++;
        }
        break;
    case 6:
        if (time >= 460.0f) {
            group->state++;
        }
        break;
    case 7:
        group->count = 90;
        group->state++;
        /* fallthrough */
    case 8:
        group->count++;
        if (group->count > 45) {
            for (i = 0; i < 8; i++) {
                if (group->middle[i].state == 2 && fn_1_7660(fn_1_61D0, &group->middle[i]) != 0) {
                    group->middle[i].state = 0;
                    Hu3DMotionSpeedSet(group->middle[i].model, 0.34f);
                    group->count = 0;
                    break;
                }
            }
        }
        if (time >= 555.0f) {
            for (i = 0; i < 8; i++) {
                if (group->middle[i].state != 2) {
                    group->middle[i].moving = 1;
                }
            }
            group->state++;
        }
        break;
    }
}

s32 fn_1_6098(void *data)
{
    M638Prop *prop;
    int done;
    done = 0;
    prop = data;
    switch (prop->state) {
    case 0:
        prop->pos.x = 100.0f * ((frandmod(10001) / 10000.0f) - 0.5f);
        Hu3DModelPosSetV(prop->model, &prop->pos);
        Hu3DModelAttrReset(prop->model, 1U);
        Hu3DModelAttrReset(prop->model, 1073741826U);
        Hu3DMotionTimeSet(prop->model, 0.0f);
        prop->state++;
        /* fallthrough */
    case 1:
        if (Hu3DMotionEndCheck(prop->model)) {
            Hu3DModelAttrSet(prop->model, 1U);
            Hu3DModelAttrSet(prop->model, 1073741826U);
            prop->state = 2;
            done = 1;
        }
        break;
    }
    return done;
}

s32 fn_1_61D0(void *data)
{
    M638Prop *prop;
    int done;
    done = 0;
    prop = data;
    switch (prop->state) {
    case 0:
        prop->pos.x = 10.0f + 100.0f * ((frandmod(10001) / 10000.0f) - 0.5f);
        Hu3DModelPosSetV(prop->model, &prop->pos);
        Hu3DModelRotSet(prop->model, -10.0f, 0.0f, 0.0f);
        Hu3DModelAttrReset(prop->model, 1U);
        Hu3DModelAttrReset(prop->model, 1073741826U);
        Hu3DMotionTimeSet(prop->model, 0.0f);
        prop->state++;
        /* fallthrough */
    case 1:
        if (prop->moving) {
            prop->pos.z += 1.0f;
        }
        Hu3DModelPosSetV(prop->model, &prop->pos);
        if (Hu3DMotionEndCheck(prop->model)) {
            Hu3DModelAttrSet(prop->model, 1U);
            Hu3DModelAttrSet(prop->model, 1073741826U);
            prop->state = 2;
            done = 1;
        }
        break;
    }
    return done;
}

s32 fn_1_6374(void *data)
{
    M638SceneSplitView *scene;
    f32 time;
    scene = data;
    scene->camera.elapsed += 1.0f;
    time = scene->camera.elapsed / 60.0f;
    if (time > 1.0f) {
        time = 1.0f;
    }
    if (scene->side == 0) {
        scene->camera.splitX = 320.0f * (1.0f - time) + 640.0f * time;
    } else {
        scene->camera.splitX = 320.0f * (1.0f - time);
    }
    if (scene->camera.elapsed >= 60.0f) {
        return 1;
    }
    return 0;
}

s32 fn_1_647C(void *data)
{
    M638TeamFinishView *team;
    f32 weight;
    f32 motionTime;
    int done;
    done = 0;
    team = data;
    switch (team->state) {
    case 0:
        motionTime = Hu3DMotionTimeGet(team->model);
        weight = (570.0f - motionTime) / 15.0f;
        if (weight > 1.0f) {
            weight = 1.0f;
        }
        if (weight < 0.0f) {
            weight = 0.0f;
        }
        team->blend = 0.3f * (1.0f - weight) + team->base * weight;
        team->output = team->source * weight;
        if (motionTime >= 570.0f) {
            if (team->variant == 0) {
                HuAudFXPlay(1895);
            } else {
                HuAudFXPlay(1896);
            }
            team->output = 0.0f;
            team->state++;
        }
        break;
    case 1:
        motionTime = Hu3DMotionTimeGet(team->model);
        weight = (600.0f - motionTime) / 30.0f;
        if (weight > 1.0f) {
            weight = 1.0f;
        }
        if (weight < 0.0f) {
            weight = 0.0f;
        }
        team->blend = 0.3f * weight;
        if (Hu3DMotionEndCheck(team->model)) {
            if (team->variant == 0) {
                HuAudFXPlay(1897);
            } else {
                HuAudFXPlay(1898);
            }
            team->state = 2;
            team->blend = 0.0f;
            done = 1;
        }
        break;
    }
    return done;
}

s32 fn_1_66C0(void *data)
{
    M638TeamFinishView *team;
    f32 time;
    team = data;
    team->elapsed += 1.0f;
    time = team->elapsed / 30.0f;
    if (time > 1.0f) {
        time = 1.0f;
    }
    team->blend = team->base * (1.0f - time);
    if (team->elapsed >= 30.0f) {
        team->state = 2;
        team->blend = 0.0f;
        return 1;
    }
    return 0;
}

s32 fn_1_6790(void *data)
{
    M638TimerWatch *watch;
    watch = data;
    watch->count++;
    if (watch->count >= 90) {
        MgTimerRecordSet(watch->timer, -1);
        return 1;
    }
    return 0;
}
