# Isolated validation marker

This temporary branch exists only to run the AI-workspace public checks against
`agent/recovery-context-workflow`. It is not a source promotion and does not
target `main`.
